IMG_NAME=revvy
ENABLE_SSH=1 # TODO do not enable SSH on production robots by default
DEPLOY_ZIP=0 # Github Actions will create a zip from our images anyway
export FIRST_USER_PASS=${FIRST_USER_PASS:-123}
